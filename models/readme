弄清楚两个东西：
1. 激活函数怎么搞，这个可以马上用到项目中
    激活函数要和训练过程配合使用。

    stem1和stem2为什么中间没有激活函数，至少有个identity吧
    答：在 forward 中 有一个relu，随着 deploy / train 会被消灭掉

    那个3是什么东西？
    答：

2. 两个卷积层的合并，这个似乎一定要遇到padding，看一下 3 * 3 可以不可以合并
    答：它的合并过程是怎么样的, 文中主要使用1*1 与 1*1 合并， 3*3合并比较困难，
        但是我发现 3*3 -> 1*1可以合并，今天写个程序来测试它。

3. 先构建RepVGG的Block，不带激活函数，将激活函数拿出来，先弄最简单的那种，之后弄得多了在构建可反复构建的Block，
主要是担心实力不足。